# Swift
.build/
.swiftpm/
Package.resolved

# IDE
.vscode
.DS_Store

# Build artifacts (distributed via GitHub Release)
mcpb/server/*
!mcpb/server/.gitkeep

# MCPB package (generated)
mcpb/*.mcpb

# Release artifacts (built locally, distributed via GitHub Release)
dist/

# Session / local state
.remember/
.claude/settings.local.json
.spectra/
openspec/.vector-search.db*
